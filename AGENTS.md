# Game-development resources

When updating `src/pages/game-dev.md`, update its `updated` frontmatter and the `https://maxarias.com/game-dev` `lastmod` entry in `src/pages/sitemap.xml.ts` to the same current UTC date.
